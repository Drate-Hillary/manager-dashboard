import 'package:flutter/material.dart';

class CustomerCommunication extends StatelessWidget {
  const CustomerCommunication({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Customer Communication Screen'),
    );
  }
}