import 'package:flutter/material.dart';

class InventorySupplies extends StatelessWidget {
  const InventorySupplies({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Inventory & Supplies Screen'),
    );
  }
}