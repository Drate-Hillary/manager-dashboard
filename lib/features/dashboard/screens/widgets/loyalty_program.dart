import 'package:flutter/material.dart';

class LoyaltyProgram extends StatelessWidget {
  const LoyaltyProgram({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Loyalty Program Screen'),
    );
  }
}