import 'package:flutter/material.dart';

class OperationsDispatch extends StatelessWidget {
  const OperationsDispatch({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Operation & Dispatch Screen'),
    );
  }
}
