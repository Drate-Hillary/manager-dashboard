import 'package:flutter/material.dart';

class SystemConfiguration extends StatelessWidget {
  const SystemConfiguration({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('System Configuration Screen'),
    );
  }
}