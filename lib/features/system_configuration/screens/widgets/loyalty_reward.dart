import 'package:flutter/material.dart';

class LoyaltyReward extends StatelessWidget {
  const LoyaltyReward({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('Loyalty Reward Widget'),
    );
  }
}